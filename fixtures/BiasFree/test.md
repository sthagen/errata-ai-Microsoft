# Bias-free communication

Configure master/slave replication, not master/subordinate.

Place the host in the DMZ, or in the demilitarized zone.

Place the host in the perimeter network instead.

The app hangs when loading, but you can hang up the phone.

Run a sanity check on the data.
